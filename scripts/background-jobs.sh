# use like bgjob <tenant> <environment> <command>
# check for ~/tmp and make if not exists
# check for ~/tmp/<tenant>.<env> and ask for password if not exists
# log in, get cookie and save to cookie-jar ~/tmp/<tenant>.<env> --silent 

# retrive all background job from /api/v1/backgroundjobs/jobs
# some interface or something to filter/search and sort through background jobs
#
# maybe write it in GO??
# https://github.com/charmbracelet/bubbletea/tree/master/examples
